new VenoBox({ 
    selector: '.mi-galeria-links', 
    numeration: true,        
    infinigall: true,
    
    // Opciones para asegurar la navegación y cierre:
    navigation: true, // Flechas de siguiente/anterior
    navKeyboard: true, // Navegación con el teclado
    overlayClose: true, // Cierre al hacer clic en el fondo (aparte de la 'X')
    
    // Otras opciones que ya tenías
    share: true,
    spinner: 'flow'
});